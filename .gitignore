__pycache__/
*.pyc
.env
results/*
!results/.gitkeep
# All rubrics are private (served only at score time). Keep them out of git.
rubrics/*
# Private full task records (prompt_context / formatting_context / rubric pointer).
data/tasks.private.jsonl
outputs/
*.tmp
workspaces/
